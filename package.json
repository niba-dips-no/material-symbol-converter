{
  "name": "material-symbol-converter",
  "version": "1.0.0",
  "description": "Convert selected Figma icons to Google Material Symbols SVG format",
  "scripts": {
    "build": "npm run build:plugin && npm run build:ui",
    "build:plugin": "esbuild src/code.ts --bundle --outfile=dist/code.js --format=iife --platform=neutral --target=es2020",
    "build:ui": "cp src/ui.html dist/ui.html",
    "watch": "esbuild src/code.ts --bundle --outfile=dist/code.js --watch --format=iife --platform=neutral --target=es2020",
    "dev": "npm run build:ui && npm run watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.91.0",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}
